# 01.b-startup-shapes-lab Checklist

## Purpose

Deliver a guided, hands-on learning sample that carries a local copy of the `01-minimal` source and uses it to compare CNCF startup shapes.
This is the `01` branch for runtime roles, after the learner has seen the
minimal selector and active Component source choices.

Stage Status:
- Current status: `DONE`
- Owner: `Codex + human`
- Update rule: `Update this block whenever a checklist item changes state.`

## Focus

- selector practice
- `command` vs `server` vs `client`
- hands-on observation
- startup-shape comparison
- no CML; this remains a Textus operation lab

## Step

- Provide a guided lab flow that helps a user compare `command`, `server`, and `client` startup shapes by operating this lab directory directly.

## Checklist

- [x] The lab clearly states that it reuses the `01-minimal` source locally
- [x] The lab explains selector format with concrete examples
- [x] The lab compares `command`, `server`, and `client` startup methods
- [x] The lab makes clear which aspects stay stable across those modes
- [x] The lab includes executable command examples
- [x] The lab directory contains local source code for hands-on execution
- [x] The lab provides helper scripts for each startup shape
- [x] `samples/01.b-startup-shapes-lab/README.md` is updated to match the lab flow

## Exit Criteria

- [x] The learning path is clear and executable
- [x] The relationship to `01-minimal` is explicit
- [x] The lab is self-contained enough for local experimentation
- [x] README is complete
