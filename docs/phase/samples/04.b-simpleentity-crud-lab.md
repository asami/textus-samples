# 02.b-simpleentity-crud-lab Checklist

## Purpose

Deliver a CRUD sample that makes the `SimpleEntity` shape visible and keeps the same Cozy/CML method as `textus-user-account`.

Stage Status:
- Current status: `DONE`
- Owner: `Codex + human`
- Update rule: `Update this block whenever a checklist item changes state.`

## Focus

- Dox-style Cozy model input
- `SimpleEntity`-based entity shape
- generated service/operation/value types
- `cozy`-driven flow
- `textus.*` / `query.*` / domain parameter split

## Step

- Add a `SimpleEntity` follow-up to `04-crud` and inspect the generated/runtime CRUD shape.

## Checklist

- [x] The sample follows the same method as `textus-user-account`
- [x] `cozy` command prerequisite is documented in user-facing docs
- [x] The sample is centered on a `SimpleEntity`-based CML model
- [x] The generated code exposes the standard `SimpleEntity` attribute groups
- [x] The sample no longer reads as hand-written CRUD repository logic
- [x] `samples/04.b-simpleentity-crud-lab/README.md` is updated to explain the difference from `04-crud`
- [x] The Cozy-driven CRUD flow is implemented
- [x] The sample documents the shared parameter namespace rule

## Exit Criteria

- [x] `cozyGenerate` succeeds
- [x] `compile` succeeds
- [x] The generated/runtime CRUD surface is confirmed
- [x] README is complete
