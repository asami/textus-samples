# 03.b-test-sync-command-lab Checklist

## Purpose

Deliver a lab that shows a runtime override for synchronous execution of an otherwise async/job-backed command.

Stage Status:
- Current status: `IN_PROGRESS`
- Owner: `Codex + human`
- Update rule: `Update this block whenever a checklist item changes state.`

## Focus

- async command by default
- sync override for test/local/debug
- separation from design-time sync

## Step

- Add a follow-up lab to `03-cqrs` for runtime/test-oriented sync execution override.

## Checklist

- [x] The lab is clearly separate from `03-cqrs`
- [x] The lab is clearly separate from `03.a-designed-sync-command-lab`
- [x] One command remains async/job-backed by default
- [x] The same command can run synchronously through explicit runtime override
- [x] `samples/03.b-test-sync-command-lab/README.md` is updated to match the implementation

## Exit Criteria

- [x] Build succeeds
- [x] CLI execution works
- [x] Default async behavior is confirmed
- [ ] Override sync behavior is confirmed
- [x] README is complete

## Note

- The override path keeps the external interface job-based.
- Runtime success for `sync-job-async-interface` is confirmed.
- The remaining gap is a stronger black-box demonstration that internal completion really happened before the job id was returned.
