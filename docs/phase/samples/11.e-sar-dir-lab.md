# 11.e-sar-dir-lab Checklist

## Purpose

Deliver a lab that demonstrates startup from expanded `sar.d` for loader/debug
inspection and contrasts it with the normal `--subsystem-dev-dir` development
root.

Stage Status:
- Current status: `DONE`
- Owner: `Codex + human`
- Update rule: `Update this block whenever a checklist item changes state.`

## Focus

- Expanded `sar.d`
- Loader/debug startup
- Selector-less descriptor inference
- Contrast with packaged `*.sar`

## Step

- Show that the same subsystem can be started from the unpacked SAR layout
  through `--subsystem-sar-dir` without also passing `--textus.subsystem`.

## Checklist

- [x] Expanded `sar.d` startup via `--subsystem-sar-dir` is implemented
- [x] The README explains the explicit role of expanded `sar.d`
- [x] `samples/11.e-sar-dir-lab/README.md` is updated to match the implementation
- [x] The run script uses selector-less expanded SAR startup

## Exit Criteria

- [x] Build succeeds
- [x] CLI execution works
- [x] Expanded `sar.d` behavior is confirmed
