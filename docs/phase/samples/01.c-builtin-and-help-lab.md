# 01.c-builtin-and-help-lab Checklist

## Purpose

Deliver a guided hands-on learning sample that introduces builtin Components, help surfaces, and runtime discovery commands around a local copy of `01-minimal`.
This is the `01` branch for runtime-provided surfaces, not a Component
authoring or CML sample.

Stage Status:
- Current status: `DONE`
- Owner: `Codex + human`
- Update rule: `Update this block whenever a checklist item changes state.`

## Focus

- builtin Components
- help-oriented surfaces
- runtime discovery commands
- `command` for development-time testing, learning, and management commands
- `client` for realistic remote usage
- `server` + `curl` for HTTP-surface observation
- no CML; this remains a Textus operation lab

## Step

- Show how a learner can distinguish sample-defined command paths from runtime-provided command surfaces by operating this lab directory directly.

## Checklist

- [x] The lab clearly states that it reuses the `01-minimal` source locally
- [x] The lab introduces builtin or framework-provided command surfaces
- [x] The lab includes `help`-style command examples
- [x] The lab includes runtime discovery command examples
- [x] The lab explains how those commands differ from sample-defined operations
- [x] The lab explains the expected `command` usage order: development-time testing, learning, management commands
- [x] The lab explains that `client` is a common operational entry point
- [x] The lab explains the role of `server` + `curl` as HTTP-surface observation
- [x] The lab provides helper scripts for builtin/help observations
- [x] `samples/01.c-builtin-and-help-lab/README.md` is updated to match the lab flow
- [x] `./run-subsystem-help.sh` is verified and recorded
- [x] `./run-component-help.sh` is verified and recorded
- [x] `./run-operation-help.sh` is verified and recorded
- [x] `./run-admin-ping.sh` is verified and recorded
- [x] `./invoke.sh` is verified and recorded
- [x] `client`-based observation is verified and recorded
- [x] `server` + `curl` observation is verified and recorded

## Exit Criteria

- [x] The builtin/help learning path is clear
- [x] The relationship to `01-minimal` is explicit
- [x] README is complete
- [x] Verification record exists and is filled with observed results
- [x] All verification checklist items above are complete
