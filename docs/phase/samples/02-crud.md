# 02-crud Checklist

## Purpose

Deliver a CRUD sample that demonstrates model-driven entity and aggregate services from CML.

Stage Status:
- Current status: `DONE`
- Owner: `Codex + human`
- Update rule: `Update this block whenever a checklist item changes state.`

## Focus

- CML model
- entity service
- aggregate service
- `cozy`-driven flow

## Step

- Replace the current hand-written CRUD direction with the `textus-user-account` style CML-driven approach.

## Checklist

- [x] The sample follows the same approach as `textus-user-account`
- [x] `cozy` command prerequisite is documented in user-facing docs
- [x] The sample is centered on CML-driven entity service
- [x] The sample is centered on CML-driven aggregate service
- [x] The sample no longer reads as hand-written CRUD repository logic
- [x] `samples/02-crud/README.md` is updated to match the redesign
- [x] The CML-driven CRUD flow is implemented

## Exit Criteria

- [x] The generated/runtime CRUD surface is confirmed
- [x] README is complete
