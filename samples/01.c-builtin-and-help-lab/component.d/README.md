# component.d

This directory stores component definition files used during local development.

This lab keeps the same minimal component source as `01-minimal`, but the main focus is not the sample operation itself.
The focus is how runtime-provided help and builtin command surfaces appear around that sample component.
