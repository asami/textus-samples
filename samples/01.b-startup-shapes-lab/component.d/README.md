# component.d

This directory stores component definition files used during local development.

This lab keeps the same minimal component structure as `01-minimal`, but changes how the process starts:

- `command`
- `server`
- `client`
