# component.d

This directory stores component definition files used during local development.

In this lab, the primary comparison is:

- `run.sh`: local compiled classes
- `invoke.sh`: packaged jar via `samples/component-repository.d`

The local component metadata is still kept here so the loading model remains visible.
